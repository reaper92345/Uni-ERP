# Database Configuration
DB_HOST=db
DB_USER=inventory_user
DB_PASS=inventory_password
DB_NAME=inventory_system

# Application Configuration
APP_ENV=development
DEBUG=true

# MySQL Root Password (for database container)
MYSQL_ROOT_PASSWORD=root_password

# Port Mappings (optional - defaults are used if not specified)
APP_PORT=8080
DB_PORT=3306
PMA_PORT=8081

# Volume Paths (optional - defaults are used if not specified)
UPLOADS_PATH=./uploads
LOGS_PATH=./logs
DB_DATA_PATH=./mysql_data
